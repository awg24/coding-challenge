var React = require("react");
var Link = require("react-router").Link;

module.exports = React.createClass({
	render: function(){
		return (
			<div>
				You've appeared to have ended up somewhere that doesn't exist.. go back
			</div>
		);
	}
});
